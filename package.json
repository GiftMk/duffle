{
	"name": "duffle",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"check": "biome lint --write --unsafe && biome check --write",
		"dev": "bun -F app dev"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.10",
		"@types/bun": "latest",
		"babel-plugin-react-compiler": "^1.0.0"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
